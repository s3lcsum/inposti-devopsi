Thank you for installing {{ .Chart.Name }}.

Your release is named {{ .Release.Name }}.

## Spring Boot API Access

{{- if .Values.ingress.enabled }}
{{- range $host := .Values.ingress.hosts }}
{{- range .paths }}
  http{{ if $.Values.ingress.tls }}s{{ end }}://{{ $host.host }}{{ .path }}
{{- end }}
{{- end }}
{{- else }}
To access the Spring Boot API:

1. Port forward the service:
   kubectl port-forward svc/{{ include "spring-api.fullname" . }} -n {{ .Release.Namespace }} 8080:8080

2. Access the API at:
   http://localhost:8080

3. Available endpoints:
   - API: http://localhost:8080/api
   - Logs: http://localhost:8081/logs  
   - SOAP: http://localhost:8082/soap
{{- end }}

## Environment Information

- Environment: {{ .Values.environment }}
- Spring Profile: {{ .Values.springProfile }}
- Replicas: {{ .Values.replicaCount }}
- Image: {{ .Values.image.repository }}:{{ .Values.image.tag }}

## Health Checks

The application uses TCP health checks on port 8080:
- Liveness probe: TCP on port 8080
- Readiness probe: TCP on port 8080

## Configuration

- ConfigMap mounted at: /app/config.json
- Random secrets generated for: DB_PASSWORD, JWT_SECRET, API_KEY, ENCRYPTION_KEY
- POD_IP environment variable available
- Pre-stop hook: wget http://localhost:8080/service/shutdown
